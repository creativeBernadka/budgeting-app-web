<div
  {{did-insert this.getChartData}}
>
  {{#if this.chartDataInstance.isRunning}}
    <ChartsLoading/>
  {{/if}}

  {{#if this.chartDataInstance.isSuccessful}}
    <MainPageCharts
            @chartData={{this.chartDataInstance.value}}
    />
  {{/if}}

</div>