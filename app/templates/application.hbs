<div class="container">
    <div class="menu-box">
        <div class="menu">
          {{#link-to "index" id="home"}}
              Home
          {{/link-to}}
          {{#link-to "account" id="account"}}
              Account
          {{/link-to}}
          {{#link-to "stats" id="stats"}}
              Statistics
          {{/link-to}}
        </div>
        <div class="add-new-transaction">
          {{#link-to "transactions" id="transactions"}}
              Transaction
          {{/link-to}}
        </div>
    </div>
    <div class="body">
      {{outlet}}
    </div>
</div>